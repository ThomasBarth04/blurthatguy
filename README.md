# blurthatguy

Python tool for blurring a target person out of media. Work in progress
(see [`todo.txt`](todo.txt)).

## Layout

- `main.py` — entry point
- `data/` — input/working data
- `requirements.txt` — dependencies

## Setup

```bash
pip install -r requirements.txt
python main.py
```